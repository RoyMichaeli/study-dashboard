# 📋 מדריך מלא לכתיבת JSON תקין למערכת הלמידה

## 🎯 **מבנה בסיסי נדרש לשיעור**

```json
{
  "שם": "שם השיעור",
  "מצגת": "כותרת המצגת או תיאור",
  "נושאים": [
    {
      "כותרת": "שם הנושא",
      "מטרות": [
        "מטרה 1",
        "מטרה 2"
      ]
    }
  ]
}
```

## ✅ **שדות חובה**

### 1. **`שם`** (מחרוזת)
- שם השיעור
- **חובה** - אם חסר, השיעור לא יתווסף
- דוגמה: `"שם": "כימיה אורגנית - אלקנים"`

### 2. **`מצגת`** (מחרוזת)  
- כותרת המצגת או תיאור קצר
- **חובה** - אם חסר, השיעור לא יתווסף
- דוגמה: `"מצגת": "הרצאה 1 - יסודות הכימיה האורגנית"`

### 3. **`נושאים`** (מערך)
- **חובה** - חייב להכיל לפחות נושא אחד
- כל נושא חייב לכלול:
  - **`כותרת`** (מחרוזת) - שם הנושא
  - **`מטרות`** (מערך מחרוזות) - רשימת מטרות הלמידה

## 🚫 **שגיאות נפוצות ופתרונות**

### 1. **תווי ציטוט לא חוקיים**
❌ **שגוי:**
```json
"ד\"ר משה כהן"
```
✅ **נכון:**
```json
"דר' משה כהן"
```
או:
```json
"ד\\\"ר משה כהן"
```

### 2. **פסיק אחרון (Trailing Comma)**
❌ **שגוי:**
```json
{
  "שם": "שיעור",
  "מצגת": "מצגת",  ← פסיק מיותר
}
```
✅ **נכון:**
```json
{
  "שם": "שיעור",
  "מצגת": "מצגת"
}
```

### 3. **תווים מיוחדים בעייתיים**
❌ **להימנע מ:**
- `$K_C$` → השתמש ב-`KC` או `K_C`
- `⇒` → השתמש ב-`->` או הסר
- `≈` → השתמש ב-`בקירוב` או `~`
- `""` (ציטוט כפול) → השתמש ב-`''` או הסר

### 4. **מטרות ארוכות מדי**
❌ **שגוי:**
```json
"מטרות": [
  "מטרה ארוכה מאוד שממשיכה לכמה שורות ויכולה לגרום לבעיות בעיבוד ולכן עדיף לקצר אותה..."
]
```
✅ **נכון:**
```json
"מטרות": [
  "להבין את המושג הבסיסי",
  "ליישם את הכללים בפועל"
]
```

## 📐 **כללי JSON כלליים**

### 1. **מבנה נכון**
- כל מפתח חייב להיות במרכאות כפולות: `"מפתח"`
- כל ערך מחרוזת חייב להיות במרכאות כפולות: `"ערך"`
- פסיק בין אלמנטים, אבל **לא אחרי האחרון**

### 2. **סוגי נתונים מותרים**
```json
{
  "מחרוזת": "טקסט",
  "מספר": 123,
  "בוליאני": true,
  "מערך": ["ערך1", "ערך2"],
  "אובייקט": {"מפתח": "ערך"},
  "ריק": null
}
```

### 3. **תווים שצריכים Escape**
```json
{
  "ציטוט": "הוא אמר: \"שלום\"",
  "נטוי אחורי": "נתיב\\תיקייה",
  "שורה חדשה": "שורה 1\nשורה 2",
  "טאב": "עמודה 1\tעמודה 2"
}
```

## 📝 **דוגמה מושלמת**

```json
{
  "שם": "כימיה פיסיקלית - שיווי משקל",
  "מצגת": "הרצאה על שיווי משקל כימי",
  "נושאים": [
    {
      "כותרת": "מבוא לשיווי משקל",
      "מטרות": [
        "להגדיר שיווי משקל דינמי",
        "להבדיל בין תגובה ישירה להפיכה",
        "להבין את תנאי שיווי המשקל"
      ]
    },
    {
      "כותרת": "קבוע שיווי משקל",
      "מטרות": [
        "לחשב קבוע שיווי משקל KC",
        "להבין את הקשר בין KC ל-KP",
        "ליישם טבלאות ICE"
      ]
    }
  ]
}
```

## 🔧 **כלים לבדיקת JSON**

### 1. **בדיקה ידנית**
- ספור מרכאות פותחות וסוגרות
- ודא שיש פסיק בין אלמנטים
- בדוק שאין פסיק אחרי האלמנט האחרון

### 2. **כלים מקוונים**
- JSONLint.com
- JSON Formatter & Validator
- VS Code עם הארכה JSON

### 3. **בדיקה במערכת**
אם ה-JSON לא עובד:
1. העתק לעורך טקסט
2. בדוק שגיאות תחביר
3. הסר תווים מיוחדים
4. קצר מטרות ארוכות

## ⚠️ **אזהרות חשובות**

1. **אל תשכח שדות חובה** - `שם`, `מצגת`, `נושאים`
2. **אל תשתמש בתווי ציטוט כפולים בתוך טקסט**
3. **אל תוסיף פסיק אחרי האלמנט האחרון**
4. **שמור על מבנה קבוע** - אל תוסיף שדות שלא קיימים במערכת
5. **השתמש בעברית** - המערכת מתוכננת לעברית

## 🎯 **טיפים לכתיבה מהירה**

1. **השתמש בתבנית**: התחל עם הדוגמה הבסיסית והחלף תוכן
2. **כתוב מטרות קצרות**: עד 10-15 מילים למטרה
3. **בדוק תחביר**: לפני הדבקה, עבור על ה-JSON פעם נוספת
4. **שמור קבצים**: שמור JSON מוצלח לשימוש עתידי

זה הכל! עם ההנחיות האלו, אתה אמור להיות מסוגל לכתוב JSON שעובד מושלם במערכת 🎯✨ 